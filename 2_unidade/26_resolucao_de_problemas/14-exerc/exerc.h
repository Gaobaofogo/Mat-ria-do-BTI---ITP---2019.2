/*
 * Returns the sum of all elements
 * above the main diagonal
 *
 * @param m[5][5] Int matrix 2x2
 * @return Int sum of elements above the main diagonal
 */
int sumAboveMainDiagonal(int *m) {
  int aux
  for (int i = 0;  i < 25; i++)
}
